# Mingle Environment Variables
# Copy this to .env.local and fill in your values

# Signaling server WebSocket URL
# Local dev:
VITE_SIGNALING_SERVER=ws://localhost:4000

# Production (after deploying backend to Render):
# VITE_SIGNALING_SERVER=wss://mingle-server.onrender.com
